// Keep `?inline` when changing this path — Takumi needs embedded image bytes at build time.
import ogIcon from "../assets/app/placeholder-app-icon.png?inline";

export const ogIconSrc = ogIcon as string;
